<?php


class RR {

    static function eiset($str){
        if(isset($str) && !empty($str)){
            return true;
       }
        else{
            // incrementation dans un tableau d'erreur
            return false;
        }
    }
}